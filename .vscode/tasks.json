{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Java with Input File",
            "type": "shell",
            "command": "cmd",
            "args": [
                "/c",
                "cd \"${workspaceFolder}\" && javac -d bin \"${file}\" && java -cp bin CorePrinciple.${fileBasenameNoExtension} < input.txt > output.txt && type output.txt"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}